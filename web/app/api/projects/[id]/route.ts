import { NextResponse } from 'next/server'
import { requireSession } from '@/lib/auth'
import { deleteProject, getProject, updateProject } from '@/lib/projects'
import { projectInputSchema, projectStatusSchema } from '@/lib/domain'
import { toErrorResponse } from '@/lib/errors'
export async function GET(_request: Request, { params }: { params: { id: string } }) { try { const project = await getProject((await requireSession()).userId, params.id); return project ? NextResponse.json({ project }) : NextResponse.json({ error: { code: 'NOT_FOUND', message: 'Project not found', category: 'validation', retryable: false } }, { status: 404 }) } catch (error) { return NextResponse.json(toErrorResponse(error), { status: 401 }) } }
export async function PATCH(request: Request, { params }: { params: { id: string } }) { try { const input = projectInputSchema.partial().extend({ status: projectStatusSchema.optional() }).parse(await request.json()); const project = await updateProject((await requireSession()).userId, params.id, input); return project ? NextResponse.json({ project }) : NextResponse.json({ error: { code: 'NOT_FOUND', message: 'Project not found', category: 'validation', retryable: false } }, { status: 404 }) } catch (error) { return NextResponse.json(toErrorResponse(error), { status: 400 }) } }
export async function DELETE(_request: Request, { params }: { params: { id: string } }) { try { const deleted = await deleteProject((await requireSession()).userId, params.id); return deleted ? new NextResponse(null, { status: 204 }) : NextResponse.json({ error: { code: 'NOT_FOUND', message: 'Project not found', category: 'validation', retryable: false } }, { status: 404 }) } catch (error) { return NextResponse.json(toErrorResponse(error), { status: 401 }) } }
