'use client'
import { useState } from 'react'
const seed=[{name:'New product workspace',type:'web_app',status:'planning',updated:'Just now'},{name:'Omega foundation',type:'agent',status:'active',updated:'2 hours ago'}]
export default function Dashboard(){const [projects,setProjects]=useState(seed); const [open,setOpen]=useState(false); const [name,setName]=useState(''); const create=()=>{if(name.trim()){setProjects([{name,type:'web_app',status:'draft',updated:'Just now'},...projects]);setName('');setOpen(false)}};return <main className="min-h-screen"><aside className="fixed hidden h-screen w-64 border-r border-line bg-panel p-6 md:block"><div className="mb-12 text-xl font-bold tracking-tight"><span className="accent">◈</span> BLINDBRAIN <span className="muted text-xs">AI</span></div><nav className="space-y-2 text-sm"><div className="rounded-lg bg-white/10 px-3 py-2">Overview</div><div className="muted px-3 py-2">Projects</div><div className="muted px-3 py-2">Activity stream</div><div className="muted px-3 py-2">Settings</div></nav><div className="absolute bottom-6 left-6 right-6 text-xs muted">Foundation workspace<br/><span className="accent">●</span> local development</div></aside><section className="p-5 md:ml-64 md:p-10"><header className="mb-10 flex items-start justify-between"><div><p className="accent mb-2 text-xs font-semibold uppercase tracking-[.25em]">Command center</p><h1 className="text-3xl font-semibold">Build with intent.</h1><p className="muted mt-2 max-w-xl">Turn ideas into verified digital creations through planning, agents, tools, and evidence.</p></div><button onClick={()=>setOpen(true)} className="rounded-lg bg-cyan px-4 py-2 text-sm font-semibold text-ink">+ New project</button></header><div className="mb-8 grid gap-4 sm:grid-cols-3"><Metric label="Projects" value={projects.length.toString()}/><Metric label="Active tasks" value="0"/><Metric label="Verification" value="Awaiting"/></div><div className="grid gap-6 xl:grid-cols-[1.4fr_1fr]"><div className="glass p-5"><div className="mb-5 flex justify-between"><h2 className="font-semibold">Your projects</h2><span className="muted text-xs">{projects.length} workspaces</span></div><div className="space-y-3">{projects.map((p,i)=><div key={i} className="flex items-center justify-between rounded-xl border border-line bg-black/10 p-4"><div><div className="font-medium">{p.name}</div><div className="muted mt-1 text-xs">{p.type.replace('_',' ')} · updated {p.updated}</div></div><span className="rounded-full bg-violet/15 px-3 py-1 text-xs text-violet">{p.status}</span></div>)}</div></div><div className="glass p-5"><h2 className="font-semibold">Activity stream</h2><div className="mt-5 space-y-5 text-sm"><Activity title="Foundation ready" detail="Architecture boundaries established"/><Activity title="Verification waiting" detail="No generated output is treated as complete"/><Activity title="Agents available" detail="Planner · Research · Coding · Testing · Debugging · Critic"/></div></div></div></section>{open&&<div className="fixed inset-0 grid place-items-center bg-black/70 p-5"><div className="glass w-full max-w-lg p-6"><h2 className="text-xl font-semibold">Create a project</h2><p className="muted mt-2 text-sm">This creates a local foundation record. It does not execute an agent.</p><input autoFocus value={name} onChange={e=>setName(e.target.value)} onKeyDown={e=>e.key==='Enter'&&create()} placeholder="Project name" className="mt-6 w-full rounded-lg border border-line bg-black/20 px-4 py-3 outline-none focus:border-cyan"/><div className="mt-5 flex justify-end gap-3"><button onClick={()=>setOpen(false)} className="muted px-4 py-2 text-sm">Cancel</button><button onClick={create} className="rounded-lg bg-cyan px-4 py-2 text-sm font-semibold text-ink">Create</button></div></div></div>}</main>}
function Metric({label,value}:{label:string,value:string}){return <div className="glass p-5"><div className="muted text-xs uppercase tracking-wider">{label}</div><div className="mt-3 text-2xl font-semibold">{value}</div></div>}
function Activity({title,detail}:{title:string,detail:string}){return <div className="border-l-2 border-cyan pl-4"><div>{title}</div><div className="muted mt-1 text-xs">{detail}</div></div>}
