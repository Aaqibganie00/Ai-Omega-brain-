'use client'
import { FormEvent, useState } from 'react'
import { signIn } from 'next-auth/react'
import Link from 'next/link'
export default function SignInPage() { const [email, setEmail] = useState(''); const [password, setPassword] = useState(''); const [error, setError] = useState(''); const submit = async (event: FormEvent) => { event.preventDefault(); setError(''); const result = await signIn('credentials', { email, password, redirect: false }); if (result?.error) setError('Invalid email or password'); else window.location.href = '/' }; return <main className="grid min-h-screen place-items-center p-5"><form onSubmit={submit} className="glass w-full max-w-md p-6"><h1 className="text-2xl font-semibold">Sign in to BLINDBRAIN AI</h1><input required type="email" value={email} onChange={(e) => setEmail(e.target.value)} placeholder="Email" className="mt-6 w-full rounded-lg border border-line bg-black/20 px-4 py-3"/><input required type="password" value={password} onChange={(e) => setPassword(e.target.value)} placeholder="Password" className="mt-3 w-full rounded-lg border border-line bg-black/20 px-4 py-3"/><button className="mt-5 w-full rounded-lg bg-cyan px-4 py-3 font-semibold text-ink">Sign in</button>{error && <p className="mt-3 text-sm text-red-300">{error}</p>}<Link href="/auth/signup" className="muted mt-5 block text-sm">Create an account</Link></form></main>}
